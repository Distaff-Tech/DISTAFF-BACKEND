import Marker = google.maps.Marker;
import Map = google.maps.Map;
import LatLng = google.maps.LatLng;
export declare class MarkerService {
    private static marker;
    static clear(): void;
    static showPlaceInMap(place: any, map: Map): void;
    static showLocationInMap(map: Map, location: LatLng, iconUrl?: string, title?: string): void;
    static createMarker(map: Map, position: LatLng, iconUrl?: string, title?: string): Marker;
    private static createIcon(iconUrl);
}
